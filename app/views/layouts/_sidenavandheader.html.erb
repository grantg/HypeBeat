<!-- page top -->
    <div id="page-header">
      <div class="container-fluid">
        <div class="row">
          <div class="col-lg-12">
            <a class="navbar-brand" href="/"><strong><i>hypeBeat</i></strong> &nbsp; <span style="font-size:12px;">Beat the hype.</span></a>
            <ul class="user-top-right-standard user-top-right nav navbar-nav">
              <li class="btn-log">
                <div class="logout-btn">
                  <% if user_signed_in? %>
                    <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
                  <% end %>
                  
                </div>
              </li>
            </ul>
            <div style="clear: both;"></div>
          </div>
        </div>
      </div>
    </div>

        <div class="profile-user-details">
          <div class="user-top-right-mobile">
            <li class="btn-log">
              <div class="logout-btn">
                <% if user_signed_in? %>
                  <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
                <% end %>
              </div>
            </li>
          </div>
        </div>
      </div>
    </div>